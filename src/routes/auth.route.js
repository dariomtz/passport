const router = require('express').Router();
const passport = require('passport');
// path: auth/

// GET /login

// GET /google/login

// GET /google/callback

// GET /verifyLogin

// GET /logout

module.exports = router;
